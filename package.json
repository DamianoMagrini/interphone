{
  "name": "interphone",
  "version": "1.0.0",
  "description": "A library that manages communication with web workers, hiding it behind a simple asynchronous interface.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "run-p clean:*",
    "clean:pre": "rimraf main worker",
    "clean:post": "rimraf out",
    "build": "run-s clean:pre build:tsc build:copy clean:post",
    "build:tsc": "tsc",
    "build:copy": "copyfiles --up 1 out/**/* ."
  },
  "author": "Damiano Magrini <damiano.magrini@gmail.com>",
  "license": "CC-BY-4.0",
  "devDependencies": {
    "copyfiles": "^2.1.1",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.0",
    "typescript": "^3.7.2"
  },
  "dependencies": {}
}
